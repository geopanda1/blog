---
title: "Stack (reshape)"
categories: [xarray]
image: "image.jpg"
---

How to reshape a multidimensional xarray object.

```python
import xarray as xr

ds = xr.Dataset(...)


ds_flat = ds.stack(sample=("x", "y"))
```
